#include <bits/stdc++.h>
#include <set>

using namespace std;

int main(){
    int n, x, i = 0; cin >> n >> x;

    int list[n]; 

    for(i = 0; i < n; i++){

        cin >> list[i];
     
    }

    sort(int list[0].begin(), int list[n - 1].end());

    pair<int, int> par;

    for(i = 0; i < n; i++){

        if(x - list[i] == ){

            par.first = list[i];

        }

    }

    for(set<int>::iterator it = list.begin(); it != list.end(); ++it){

		

	}

   

    return 0;
}