#include <bits/stdc++.h>

using namespace std;

int main(){
    int N, i=0; cin >> N;
    vector<string> texto;
    string linha;

    while(N != 0){
        
        texto.resize(N);

        for(i=0; i<N; i++){
            
            texto.push_back(linha);
             
        }

        cin >> N;
    }

    return 0;
}