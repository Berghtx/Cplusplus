#include <bits/stdc++.h>

using namespace std;

int main(){
    int N, C, T, i=0;

    cin >> N >> C >> T;

    string nomes[C];

    for(i=0; i<C; i++){ // A cada três cavalos eu represento uma relação (Pai, Mãe e filho)
        cin >> nomes[i];
    }



    return 0;
}